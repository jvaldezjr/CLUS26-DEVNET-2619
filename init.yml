---
# DEPRECATED: Use playbooks/site.yml for the full workflow, or run individual playbooks
# under playbooks/ (discover_firmware.yml, schedule_upgrade.yml, etc.).
#
# Example:
#   ansible-playbook playbooks/discover_firmware.yml -e test_network_id=YOUR_NETWORK_ID
#   ansible-playbook playbooks/schedule_upgrade.yml -e workflow_run_id=20250604T120000Z
#
# See README.md for Semaphore setup and workflow state details.
- name: Deprecated monolithic playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Redirect to modular playbooks
      ansible.builtin.fail:
        msg: >-
          init.yml is deprecated. Run playbooks/site.yml or individual playbooks in playbooks/.
          See README.md.
